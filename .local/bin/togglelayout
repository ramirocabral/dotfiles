#!/bin/sh

current_layout=$(swaymsg -t get_inputs | grep -m 1 -E 'xkb_active_layout_name' | awk '{print $2}' | tr -d '"')

if [[ "$current_layout" == "English" ]]; then
    new_layout="es"
else
    new_layout="us"
fi

swaymsg input type:keyboard xkb_layout "$new_layout"
