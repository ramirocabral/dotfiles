#!bin/sh

#execute as root

#start bluetooth service using systemd
systemctl start bluetooth-service

#capture the output of pactl list sinks
sink_info=$(pactl list sinks | grep -m1 bluez | awk '{print $2}')


pactl set-default-sink "$sink_info"

