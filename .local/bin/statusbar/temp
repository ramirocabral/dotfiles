#!/bin/sh

# i3blocks CPU temperature block. Sensor detection lives in `cputemp`.

temp=$("$(dirname "$0")/cputemp") || exit 0

echo "TEMP $temp°C"
echo "TEMP $temp°C"

RED=${RED:-#FF0000}

if [ "$temp" -gt 65 ]; then
    echo "$RED"
fi
